undici:
  jobs:
    - versions:
        include: ">=5.12.0 <6"
        mode: max-7
      node: '>=14'
      commands: npm run test
    - versions:
        include: ">=6 <7"
        mode: max-7
      node: '>=18'
      commands: npm run test
