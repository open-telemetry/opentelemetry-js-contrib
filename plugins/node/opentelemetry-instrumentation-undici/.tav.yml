undici:
  jobs:
    - versions: ">=5 <6"
      node: '>=14'
      commands: npm run test
    - versions: ">=6 <7"
      node: '>=18'
      commands: npm run test

  # Fix missing `contrib-test-utils` package
  pretest: npm run --prefix ../../../ lerna:link