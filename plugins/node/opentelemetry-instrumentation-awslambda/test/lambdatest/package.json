{
  "name": "lambdatest",
  "version": "0.0.0",
  "description": "Lambda handlers used in unit tests. Patching infrastructure only supports packages in node_modules so we copy this package there for patching."
}
