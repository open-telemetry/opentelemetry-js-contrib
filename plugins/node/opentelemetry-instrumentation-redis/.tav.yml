redis:

  jobs:
    - versions: "^2.6.0"
      commands: npm run test:v2
    - versions: "^3.0.0"
      commands: npm run test:v3
    - versions: "^4.0.0"
      commands: npm run test:v4

  # Fix missing `contrib-test-utils` package
  pretest: npm run --prefix ../../../ lerna:link
