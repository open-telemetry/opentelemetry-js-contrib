services:

  # OracleDB 

  # oracledb:
  #   image: gvenzl/oracle-free:23-slim
  #   container_name: oracle-test
  #   environment:
  #     - ORACLE_PASSWORD=secret
  #   ports:
  #     - "1521:1521"
  #   volumes:
  #     - ./oracledb/init.sql:/docker-entrypoint-initdb.d/init.sql
  #   command:
  #     [
  #       "bash",
  #       "-c",
  #       "sleep 60 && sqlplus system/secret@localhost:1521/FREEPDB1 as sysdba @/docker-entrypoint-initdb.d/init.sql"
  #     ]
  #   healthcheck:
  #     test: ['CMD-SHELL', 'sqlplus', 'system/secret@FREEPDB1']
  #     retries: 10

  # Zipkin

  # zipkin-all-in-one:
  #   image: openzipkin/zipkin:latest
  #   container_name: zipkin
  #   networks:
  #     - monitor
  #   ports:
  #     - "9411:9411"
  #   depends_on:
  #     oracledb:
  #       condition: service_healthy

  # Collector

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.61.0
    networks:
      - monitor
    command: [ "--config=/etc/otel-collector-config.yaml" ]
    volumes:
      - ./collector/otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - "1888:1888" # pprof extension
      - "8888:8888" # Prometheus metrics exposed by the collector
      - "8889:8889" # Prometheus exporter metrics
      - "13133:13133" # health_check extension
      - "4317:4317" # OTLP gRPC receiver
      - "55679:55679" # zpages extension
    # depends_on:
    #   oracledb:
    #     condition: service_healthy
  # Prometheus

  prometheus:
    image: quay.io/prometheus/prometheus:v2.34.0
    networks:
      - monitor
    container_name: prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    volumes:
      - ./prometheus/prometheus.yaml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    # depends_on:
      # oracledb:
      #   condition: service_healthy
  # Grafana

  grafana:
    image: grafana/grafana:10.2.0
    networks:
      - monitor
    container_name: grafana
    volumes:
      - ./grafana/grafana.ini:/etc/grafana/grafana.ini
      - ./grafana/provisioning/:/etc/grafana/provisioning/
    ports:
      - "3000:3000"
    # depends_on:
    #   oracledb:
    #     condition: service_healthy

networks:
  monitor:
    driver: bridge
