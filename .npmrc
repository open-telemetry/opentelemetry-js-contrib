lockfile-version=3
# Note: `test-all-versions` runs opt out of this via `NPM_CONFIG_MIN_RELEASE_AGE=0`, as
# `tav` resolves the versions under test from the registry and would otherwise fail on
# any release that is younger than this.
min-release-age=3
