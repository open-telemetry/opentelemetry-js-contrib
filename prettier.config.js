/* eslint-disable license-header/header */
module.exports = {
  // the repo is half with parens and half without
  // which do we want?
  arrowParens: 'avoid',
  printWidth: 80,
  trailingComma: 'es5',
  tabWidth: 2,
  semi: true,
  singleQuote: true,
};
